export class DestinationModel {

    constructor(public id: string | null, public name: string, public country: string, public description: string, public imageUrl: string, public userId: string) {
        
    }

}
